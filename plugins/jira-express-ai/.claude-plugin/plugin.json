{
  "$schema": "https://json.schemastore.org/claude-code-plugin.json",
  "name": "jexpress",
  "displayName": "JiraExpressAI",
  "description": "Orchestrates PDE AI-Work Jira tickets end-to-end (discovery, implementation, review, merge), progressing ticket status automatically and pausing at human review gates until the developer advances it.",
  "author": {
    "name": "PDE Team"
  },
  "userConfig": {
    "ATLASSIAN_EMAIL": {
      "type": "string",
      "title": "Atlassian email",
      "description": "Email address for Atlassian/Jira API authentication.",
      "required": true
    },
    "ATLASSIAN_API_TOKEN": {
      "type": "string",
      "title": "Atlassian API token",
      "description": "API token from https://id.atlassian.com/manage-profile/security/api-tokens",
      "required": true,
      "sensitive": true
    },
    "REPOS_DIR": {
      "type": "string",
      "title": "Repo clones directory",
      "description": "Absolute path to the directory containing this project's repo clones. Optional — if unset, defaults to the orchestrator's current working directory (repo clones expected as direct subfolders of it).",
      "required": false
    },
    "CONFLUENCE_SPACE_KEY": {
      "type": "string",
      "title": "Confluence space key",
      "description": "Confluence space where ticket artifact pages are synced. Defaults to PDE if unset.",
      "required": false
    },
    "CONFLUENCE_PARENT_PAGE_ID": {
      "type": "string",
      "title": "Confluence parent folder/page ID",
      "description": "ID of the Confluence folder or page under which each ticket's parent page is created. Defaults to 5148311567 if unset.",
      "required": false
    }
  }
}
