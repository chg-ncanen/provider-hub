# Ownership rules for provider-hub repository
# GitHub's CODEOWNERS: more specific paths override broader ones
# See: https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners

# Default: all repo changes require review from team maintainers
* @ncanen

# plugins/ and shared/ are team-owned — add a line per area as new ones land
# (currently just pde; e.g. add `shared/provider/ @someone` when that area starts)
plugins/ @ncanen
shared/ @ncanen

# GitHub metadata
.github/ @ncanen
